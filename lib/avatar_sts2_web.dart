// Web plugin registration for Tavus Avatar Flutter
// This file is required for Flutter web plugin support but doesn't need implementation
// since LiveKit Client SDK already handles web platform specifics

class TavusAvatarFlutterWeb {
  static void registerWith(dynamic registrar) {
    // No-op - LiveKit Client SDK handles all web-specific implementation
    // This file exists to satisfy Flutter's web plugin requirements
  }
}